export enum EnemySpawnState {
    IDLE,
    SPAWNING,
    WAIT_CLEAR,
    BOSS,
    END
}

export enum PlayState {
    JOIN,
    PLAYING,
    PAUSE,
    RESUME,
    END,
    QUIT
}

export enum EnemyState {
    IDLE,
    WALK,
    ATTACK,
    DIE,
}